---
- name: Windows playbook
  hosts: all
  gather_facts: no

  vars:
    names:
      - Rajneesh
      - Chris
      - Gareth

  tasks:

  - name: Linux
    debug: msg="{{ item }}"
    loop:
    - Item 1
    - Item 2
